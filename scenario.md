# Daily Rehearsal Scenario

> 스마트 거울 앞에서 내일의 상황을 고르고, 내일의 모습을 입어보고, AI와 시뮬레이션한 뒤 티켓으로 가져가는 체험

## 1. 체험 정의

Daily Rehearsal은 전시장 방문자가 내일 마주할 상황을 제스처로 선택하고, 짧은 브리핑과 재질문을 통해 context를 채운 뒤, 내일 입어볼 모습을 선택하고 AI와 시뮬레이션하는 오프라인 체험입니다.

마지막에는 누적 context, 선택한 옷, 대화 결과, 녹화 영상을 묶은 티켓과 QR을 제공합니다. QR을 스캔하면 영상과 티켓을 다운로드할 수 있습니다.

## 2. 전체 흐름

```text
타입 선택 → 브리핑 → 재질문 → 옷 입히기 → 시뮬레이션 → 티켓 발급
```

## 3. 화면별 경험

### 1. 타입 선택 화면 (새로 만들어져야함.)

사용자는 스마트 거울 앞에 서서 소개팅, 발표, 미팅 등 상황 타입을 제스처로 선택합니다.

화면은 타입 카드를 보여주고, 손 좌우 이동으로 후보를 바꾸며, 손바닥 유지 같은 확정 제스처로 타입을 선택합니다.

선택이 확정되면 세션이 시작됩니다.

### 2. 브리핑 화면

선택된 타입에 맞는 고정 브리핑과 예시 답변을 보여줍니다.

예시:

```text
내일 소개팅이 있어요.
처음 만났을 때 어색하지 않게 보이고 싶고,
너무 꾸민 느낌은 피하고 싶어요.
```

사용자는 예시를 보고 자신의 상황을 말합니다.

### 3. 재질문 화면

프론트는 Web Speech API로 사용자 발화를 transcript로 확정하고 백엔드로 보냅니다.

백엔드는 타입별 slot 정의에 따라 AI로 답변을 정규화하고, 채워진 context를 세션에 누적합니다.

빈 slot이 있으면 백엔드는 재질문 리스트를 한 번에 내려줍니다.

예시:

```text
- 어떤 모습으로 보이고 싶나요?
- 가장 걱정되는 순간은 언제인가요?
- 상대는 어떤 분위기일 것 같나요?
```

사용자는 한 번에 답변합니다. 답변 하나에서 여러 slot이 채워질 수 있습니다.

재질문 라운드 수가 `max_attempt`를 넘으면 남은 slot은 default로 채웁니다. AI가 실패해도 default로 처리해 다음 화면으로 넘어갑니다.

### 4. 옷 입히기 화면

백엔드는 선택 타입과 context를 기반으로 옷 목록과 Decart URI를 만듭니다.

프론트는 Decart WebRTC에 직접 연결해 첫 번째 옷을 자동으로 입힌 모습을 보여줍니다.

사용자는 타입 선택과 같은 제스처 컴포넌트로 옷을 넘기고 확정합니다.

이 화면부터 MediaRecorder 녹화가 시작됩니다. 녹화는 시뮬레이션 종료까지 이어집니다.

### 5. 시뮬레이션 화면

AI 상대와 고정된 `N`턴을 진행합니다.

각 턴은 두 단계입니다.

1. 사용자가 답변하면 백엔드가 판정과 피드백을 JSON으로 반환합니다.
2. 성공/실패 피드백이 먼저 화면에 표시되고, 다음 상대 발화는 SSE로 스트리밍됩니다.

성공하면 다음 턴으로 넘어갑니다. 실패하면 같은 턴을 다시 시도합니다. 실패 횟수는 제한하지 않습니다.

AI 호출 실패 시에는 다음 fallback을 사용합니다.

- 판정 실패: 실패 처리와 “다시 시도해보세요” 피드백
- 다음 발화 실패: 상황별 고정 상대 발화

### 6. 티켓 발급 화면

녹화를 종료하고 영상을 업로드합니다.

백엔드는 세션에 누적된 context, 선택한 옷, 시뮬레이션 로그, 피드백, 영상 URL을 바탕으로 티켓을 생성합니다.

화면은 티켓과 QR을 표시합니다. QR은 다운로드 페이지 URL입니다.

사용자는 QR을 스캔해 모바일에서 영상과 티켓을 받을 수 있습니다.

## 4. 대표 예시

### 소개팅

타입 선택: 소개팅

브리핑:

```text
내일 소개팅이 있는데 처음 만났을 때 어색할까 봐 걱정돼요.
너무 꾸민 느낌보다는 자연스럽고 단정하게 보이고 싶어요.
```

채워지는 context 예:

- `situation_type`: date
- `desired_persona`: warm_natural
- `critical_moment`: 첫 인사와 첫 대화
- `outfit_direction`: neat_casual

티켓 예:

```text
내일의 리허설 티켓

상황:
소개팅 첫 만남

오늘 입어본 모습:
과하지 않은 단정한 캐주얼

내일의 한 문장:
조금 일찍 도착해서 여유 있게 시작하기
```

### 발표

타입 선택: 발표

브리핑:

```text
내일 팀 발표가 있는데 질문을 받으면 말이 꼬일 것 같아요.
침착하고 신뢰감 있게 보이고 싶어요.
```

시뮬레이션:

- AI 상대가 예상 질문을 던짐
- 사용자는 답변
- 백엔드는 성공/실패와 피드백을 먼저 표시
- 다음 질문 또는 반응은 SSE로 스트리밍

## 5. 포함 범위

- 제스처 기반 타입 선택
- 타입별 고정 브리핑
- 프론트 STT
- slot context 수집과 재질문
- AI 정규화와 fallback default
- Decart WebRTC preview
- 제스처 기반 옷 교체
- MediaRecorder 녹화
- AI 판정/피드백 JSON API
- 다음 상대 발화 SSE
- 영상 업로드
- 티켓 생성
- QR 다운로드 페이지

## 6. 제외 범위

- 개인 일정 연동
- 캘린더, 이메일, 메신저 분석
- 무제한 자유 대화
- 복잡한 계정/히스토리 시스템
- 장문 코칭 리포트
- 실시간 길찾기 정확도 보장
- 전시 운영을 멈추게 하는 hard failure
