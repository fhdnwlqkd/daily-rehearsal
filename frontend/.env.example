# 실제 값은 .env.local(gitignore됨)에 넣는다. 이 파일에는 변수 이름만 남긴다.
# ⚠️ 아래 변수들은 서버 전용 시크릿 — NEXT_PUBLIC_ 접두사를 붙이면
# 브라우저 번들에 노출되므로 절대 붙이지 말 것. (프록시 라우트만 읽는다)

# 백엔드 실서버 주소 (IP·포트는 시크릿 — 팀 내부에서 공유받는다)
API_BASE_URL=

# 백엔드 인증 키 (프록시가 x-api-key 헤더로 첨부)
API_KEY=
